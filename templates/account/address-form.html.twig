{% extends 'base.html.twig' %}

{% block title %}ajouter une addresse{% endblock %}

{% block body %}


<div class="container mb-5">

    <div class="d-flex justify-content-between">
        <div><h1>Gérer mon adresse</h1></div>
    </div>
    
    {{form_start(form)}}
        <div class="row">
            <div class="col-md-4">{{form_row(form.name)}}</div>
        </div>
        <div class="row">
            <div class="col-md-4">{{form_row(form.firstname)}}</div>
            <div class="col-md-4">{{form_row(form.lastname)}}</div>
            <div class="col-md-4">{{form_row(form.company)}}</div>
        </div>
        <div class="row">
            <div class="col-md-12">{{form_row(form.address)}}</div>
            <div class="col-md-4">{{form_row(form.postal)}}</div>
            <div class="col-md-4">{{form_row(form.city)}}</div>
            <div class="col-md-4">{{form_row(form.country)}}</div>
        </div>
        <div class="row">
            <div class="col-md-4">{{form_row(form.phone)}}</div>
        </div>
        <div class="d-flex justify-content-end">
            <div class="">{{form_row(form.submit)}}</div>
        </div>

    {{form_end(form)}}

    <br><br>
    <a href="{{ path('app_account_address')}}" class="btn btn-info">Retour</a>
    

</div>
{% endblock %}
