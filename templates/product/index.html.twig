{% extends 'base.html.twig' %}

{% block title %}eshop produits{% endblock %}

{% block body %}


    <h1>Nos Produits</h1>

    <div class="row">
        {% for product in products %}


            <div class="col-4 mb-3" style="text-align: center;">
                <div class="product-item">
                    <img src="uploads/{{product.illustration}}" class="img-fluid" style="max-height: 200px;" alt="{{product.name}}">
                    <div class="card-body">
                        <h5 class="card-title">{{product.name}}</h5>
                        <span>{{product.subtitle}}<span>
                        <br>
                        <span>{{ (product.price / 100) | number_format(2, ',', '.')}} €<span>
                    </div>
                </div>
            </div>
        {% endfor %}
    
{% endblock %}
 